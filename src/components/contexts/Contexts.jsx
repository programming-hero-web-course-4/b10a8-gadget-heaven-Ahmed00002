import { createContext } from "react";

export const gadgetDtls = createContext();

export const AllGadgetsData = createContext();

export const userData = createContext();
export const cartDtls = createContext();
export const wishlistDtls = createContext();
